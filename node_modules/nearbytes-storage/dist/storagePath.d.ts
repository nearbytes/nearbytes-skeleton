/**
 * Default storage directory resolution.
 */
/**
 * Returns the primary local storage root directory. If NEARBYTES_STORAGE_DIR is set,
 * uses that; otherwise uses the default local path ($HOME/nearbytes/local).
 */
export declare function getDefaultStorageDir(): string;
/**
 * Returns the parent Nearbytes storage container that groups provider-specific roots.
 */
export declare function getDefaultStorageHomeDir(): string;
export declare function getDefaultRuntimeHomeDir(): string;
export declare function getProviderStorageFolderName(provider: string): string;
export declare function resolveStorageHomeDir(localStorageDir: string): string;
//# sourceMappingURL=storagePath.d.ts.map