/**
 * Creates a Volume from a secret
 * This is a factory function, not a constructor
 */
export function createVolume(secret, publicKey, path) {
    return {
        publicKey,
        secret,
        path,
    };
}
//# sourceMappingURL=volume.js.map