import type { StorageBackend } from '../types/storage.js';
export interface StoredPathRecord {
    path: string;
    data: Uint8Array;
    updatedAt: number;
}
export interface InMemoryPathRecordStore {
    files: Map<string, StoredPathRecord>;
    directories: Set<string>;
}
export interface PathRecordStore {
    putRecord(path: string, data: Uint8Array): Promise<void>;
    getRecord(path: string): Promise<StoredPathRecord | null>;
    deleteRecord(path: string): Promise<void>;
    putDirectory(path: string): Promise<void>;
    hasDirectory(path: string): Promise<boolean>;
    listStoredPaths(): Promise<string[]>;
}
export declare function normalizeStoragePath(path: string): string;
export declare function normalizeStorageDirectoryPath(path: string): string;
export declare function createInMemoryPathRecordStore(initial?: Partial<InMemoryPathRecordStore>): InMemoryPathRecordStore;
export declare function createInMemoryPathRecordStoreAdapter(store: InMemoryPathRecordStore): PathRecordStore;
export declare class PathRecordStorageBackend implements StorageBackend {
    private readonly store;
    constructor(store: PathRecordStore);
    writeFile(path: string, data: Uint8Array): Promise<void>;
    readFile(path: string): Promise<Uint8Array>;
    listFiles(directory: string): Promise<string[]>;
    createDirectory(path: string): Promise<void>;
    exists(path: string): Promise<boolean>;
    deleteFile(path: string): Promise<void>;
}
//# sourceMappingURL=pathRecord.d.ts.map